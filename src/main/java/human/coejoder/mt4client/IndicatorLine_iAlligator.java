package human.coejoder.mt4client;

/**
 * Indicator line identifiers used in iAlligator() indicator.
 */
public enum IndicatorLine_iAlligator {
    GATORJAW(1),
    GATORTEETH(2),
    GATORLIPS(3);

    public final int id;

    IndicatorLine_iAlligator(int id) {
        this.id = id;
    }
}
